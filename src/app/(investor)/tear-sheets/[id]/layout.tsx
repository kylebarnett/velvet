import type { Metadata } from "next";

export const metadata: Metadata = { title: "Tear Sheet | PostSig" };

export default function TearSheetDetailLayout({ children }: { children: React.ReactNode }) {
  return children;
}

import type { Metadata } from "next";

export const metadata: Metadata = { title: "Edit Tear Sheet | Velvet" };

export default function EditTearSheetLayout({ children }: { children: React.ReactNode }) {
  return children;
}

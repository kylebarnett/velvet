import type { Metadata } from "next";

export const metadata: Metadata = { title: "New Tear Sheet | Velvet" };

export default function NewTearSheetLayout({ children }: { children: React.ReactNode }) {
  return children;
}

import type { Metadata } from "next";

export const metadata: Metadata = { title: "Tear Sheets | PostSig" };

export default function TearSheetsLayout({ children }: { children: React.ReactNode }) {
  return children;
}

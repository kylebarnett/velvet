import type { Metadata } from "next";

export const metadata: Metadata = { title: "Investors | PostSig" };

export default function InvestorsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
